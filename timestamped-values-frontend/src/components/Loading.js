import React from 'react';

const Loading = () => {
  const loadingStyle = {
    fontSize: '18px',
    fontWeight: 'bold',
    marginTop: '20px',
  };

  return <div style={loadingStyle}>Loading...</div>;
};

export default Loading;